import 'package:flutter/material.dart';

class TakeCall extends StatelessWidget {
  const TakeCall({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(child: Column(children: [Text("Calling page")])),
    );
  }
}